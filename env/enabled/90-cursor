#! /bin/bash

# Cursor Agent CLI — `agent` / `cursor-agent` (https://cursor.com/docs/cli/installation)
# Installed via: curl https://cursor.com/install -fsS | bash
export PATH="$HOME/.local/bin:$PATH"
